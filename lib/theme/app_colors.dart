import 'package:flutter/material.dart';

class AppColors {
  // Brand colors from the 7me Portuguese portal
  static const Color primaryBlue = Color(0xFF1D51A1);
  static const Color skyBlue = Color(0xFF048EDD);
  static const Color deepBlue = Color(0xFF1D3D87); // Gradient start
  
  // Neutral colors
  static const Color gray = Color(0xFF7A7F85);
  static const Color lightGray = Color(0xFFF2F2F2);
  static const Color white = Colors.white;

  // Dark theme colors (indigo/violet background & surface)
  static const Color darkBg = Color(0xFF110C1D);
  static const Color darkSurface = Color(0xFF1A1230);
  static const Color darkBorder = Color(0xFF251E3D);

  // Status/Functional colors matching the 7me palette
  static const Color success = Color(0xFF2AA275);
  static const Color error = Color(0xFFFF3D65);
  static const Color warning = Color(0xFFFCB900);
}